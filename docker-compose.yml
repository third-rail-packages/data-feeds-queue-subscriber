version: '2'

services:
  app:
    build:
      context: ./docker/app
    environment:
      - PHP_IDE_CONFIG=serverName=trainjunkies_packages_queue_subscriber
    working_dir: /var/www
    env_file: .env
    volumes:
        - .:/var/www